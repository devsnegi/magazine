import React, { useContext } from "react";

export const Header = () => {
  //   const {  } = useContext(BookContext);
  return (
    <div className="navbar">
      <h1 className="app-title">Magazine App</h1>
      <div className="my-subscription">My Subscription</div>
    </div>
  );
};
